type User {
  avatar: String!
  bio: String
  cover: String
  id: Int
  name: String
  photo: String
  playlists: [Playlist!]!
  username: String
}

type Query {
  getUser(id: Int!, username: String!): User!
  allUsers: [User!]!
}
