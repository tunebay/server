type Playlist {
  artist: User!
  artwork: String
  id: Int!
  permalink: String!
  price: Float
  supporters: [User!]!
  title: String!
  tracks: [Track!]!
}

type Query {
  getPlaylist(id: Int!): Playlist!
  allPlaylists: [Playlist!]!
}
